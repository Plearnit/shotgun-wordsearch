
export enum LibraryTypes {
    trivia,
    math,
    vocab,
    board
}

export enum ProblemTypes {
    generator = 'gen',
    prompt = 'pro'
}

export enum MathGenFunctions {
    addition = 'add',
    subtraction = 'sub',
    division = 'div',
    multiplication = 'mul'
}